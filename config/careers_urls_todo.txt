# Fill in a careers page URL for any of these you know, one per line, in the
# same format: Company Name | https://example.com/careers
# Leave the part after '|' blank to skip a company for now -- it'll just stay
# as-is. Send the whole file back (or paste your edited lines in chat) and
# they'll get merged into config/companies.yaml automatically:
#   python -m job_search_agent.apply_careers_urls --apply config/careers_urls_todo.txt
#
# Each line below also carries the reason it's unresolved, as a comment.

# cala.ai is their site, but they hire via a direct email inquiry, not listed job postings -- nothing to crawl.
CALA | 

# Confirmed: just has an email address, not listed job postings -- nothing to crawl.
Fictive Kin | 

# User believes this may now be called "Stitch" under Google, but unconfirmed and no separate careers URL known.
Galileo AI | 

# User also could not find a public careers page.
Maximum Effort (Ryan Reynolds) | 

# User also could not find a public careers page.
ShadowMachine | 

# Only found via LinkedIn, which is excluded from crawling per policy -- no other known careers URL.
Society6 | 

# User also could not find a careers page.
Spring Bank | 

# Confirmed: just has an email address, not listed job postings -- nothing to crawl.
Tahzoo | 

# Acquired by SRI International in 2023 (try sri.com/careers); also specifically Palo Alto-based, so unlikely to pass the location filter even if crawled.
Xerox PARC | 

